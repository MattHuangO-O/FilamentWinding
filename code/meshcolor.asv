function [color] = meshcolor(n)
colorR=[1,0,1];
colorG=[0,1,0];
colorB=[1,1,0];
length=round(n/length(colorR));
for j=1:length(colorR)-1
    R((j-1)*length+1,j*length)=linspace(colorR
color(:,:,1)=R;
color(:,:,2)=G;
color(:,:,3)=B;
end